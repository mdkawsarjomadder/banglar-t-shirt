import React, { useContext } from 'react';

const Special = ({ring}) => {
    
      // const aginty = useContext(RingContaxt)
      
      return (
            <div>
                  <h2>Special</h2>
                  <p> <small >RIng: {ring}</small></p>
            </div>
      );
};

export default Special;